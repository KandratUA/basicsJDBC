SELECT c.NAME, DATEDIFF('MONTH', p.START_DATE, p.FINISH_DATE) AS monthCount
FROM project p
JOIN client c ON p.CLIENT_ID = c.ID
WHERE DATEDIFF('MONTH', p.START_DATE, p.FINISH_DATE) = (
    SELECT MAX(DATEDIFF('MONTH', START_DATE, FINISH_DATE))
    FROM project
);
